# Internal Testing Notes

## Memory Mode Gray Switch (Phase 5A)

- Default mode: `summary`.
- Toggle path: `通用设定` -> enable `显示 Debug 按钮` -> tap `DEBUG` -> press `记忆: 摘要/表格`.
- Effect: switch is immediate; prompt injection follows the selected mode (summary vs. memory table).
- This switch is for internal testing only; keep it hidden from end users.
